[package]
name = "spatial"
version = "0.1.0"
edition = "2021"
description = "Eisenstein layout intelligence — page graph, grid snap, document structure analysis"

[dependencies]
eisenstein = { version = "0.3", features = ["snap", "std"] }
snapkit = "0.1"
nalgebra = "0.33"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
